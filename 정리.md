# SLLM_visualize 구현 정리

> 목적: 지금까지 구현된 것과, 특히 프론트엔드 애니메이션이 "왜 이 순서·이 방식으로" 동작하는지를
> 나중에 다시 봐도 이해할 수 있도록 정리한 문서. 코드의 주석에 이미 적힌 "이유"를 최대한
> 살려서 옮겼다.

## 목차

1. [기획 개요 (요약)](#1-기획-개요-요약)
2. [현재 파일 구조](#2-현재-파일-구조)
3. [백엔드: 무엇을 계산하는가](#3-백엔드-무엇을-계산하는가)
4. [백엔드 → 프론트엔드 데이터 스키마](#4-백엔드--프론트엔드-데이터-스키마)
5. [프론트엔드 전체 구조](#5-프론트엔드-전체-구조)
6. [애니메이션 핵심 개념: "빛(hero) 하나 + 따라오는 카메라"](#6-애니메이션-핵심-개념-빛hero-하나--따라오는-카메라)
7. [실행 흐름 한눈에 보기 (`play()`)](#7-실행-흐름-한눈에-보기-play)
8. [0단계: 입력 → 디코이 전환](#8-0단계-입력--디코이-전환)
9. [`introZone` — 도입부(문장 확대 → 레일 스윕)](#9-introzone--도입부문장-확대--레일-스윕)
10. [`tokenZone` — 01 TOKENIZATION (7단계)](#10-tokenzone--01-tokenization-7단계)
11. [`embeddingZone` — 02 EMBEDDING FIELD](#11-embeddingzone--02-embedding-field)
12. [`decodeZone` — 03 NEXT TOKEN (스텝 반복)](#12-decodezone--03-next-token-스텝-반복)
13. [`finaleZone` — 05 DETOKENIZATION](#13-finalezone--05-detokenization)
14. [실패 처리 · 리셋](#14-실패-처리--리셋)
15. [레거시 파일: `lstm.*`](#15-레거시-파일-lstm)
16. [현재 상태 요약 / 남은 것](#16-현재-상태-요약--남은-것)

---

## 1. 기획 개요 (요약)

원문: [`프로젝트_기획서.md`](./프로젝트_기획서.md)

- 핵심 메시지: **"AI는 답을 한 번에 만드는 게 아니라, 한 조각(토큰)씩 이어 붙인다."**
- 레퍼런스는 LSTM 회로도 스타일이지만, 실제 구현 대상은 **트랜스포머 기반 sLLM**.
- 관객 경험 흐름(기획 원안): 토큰화 → 임베딩 → 어텐션 흐름 → 다음 토큰 후보 경쟁 → 선택 → 반복 → 답변 완성.
- 설계 원칙: 프리렌더 모드와 실시간 모드가 **동일한 JSON 스키마**를 쓴다 (지금 구현은 실시간 모드만 있고, 프리렌더 배치 스크립트는 아직 없음 — `generate.py`가 그 역할을 겸할 수 있는 구조로 짜여 있음).
- 비주얼 톤: 검정 배경 + 모노크롬 + 글로우, 지금 값이 흐르는 요소만 빛나게.

지금 구현은 이 기획서의 ①②④ 단계(토큰화/임베딩/다음 토큰 후보 경쟁)를 실제로 만들었고,
③ "어텐션 흐름(레이어 사이 빛나는 선)" 시각화는 **데이터는 이미 뽑고 있지만(`attended` 필드)
화면에 아직 그리지 않는 상태** (프론트에서 미사용). ⑥ 답변 완성은 `finaleZone`으로 구현됨.

---

## 2. 현재 파일 구조

```
SLLM_visualize/
├── 프로젝트_기획서.md        기획 원안
├── 정리.md                  (이 문서)
├── backend/
│   ├── server.py            정적 파일 서빙 + /api/generate 엔드포인트
│   └── generate.py          실제 모델 추론, 스텝별 데이터 계산 → JSON
└── frontend/
    ├── index.html           현재 사용하는 유일한 화면 (Token Current)
    ├── script.js            애니메이션/상태 전체 로직 (1145줄)
    ├── style.css            index.html용 스타일 (tx- 접두사 클래스들)
    ├── lstm.html            레거시 — index.html로 즉시 리다이렉트만 함
    ├── lstm.js / lstm.css   레거시 — lstm.html이 안 쓰이므로 죽은 코드
    └── lib/three.module.js  현재는 참조하는 곳 없음 (three.js 미사용, SVG로 구현됨)
```

기획서의 "three.js/p5.js vs TouchDesigner/Unity" 갈림길은 **순수 SVG + Vanilla JS**로
결정되어 구현된 상태 (three.js는 설치돼 있지만 실제로 import되지 않음).

---

## 3. 백엔드: 무엇을 계산하는가

### 3.1 `server.py`

- Python 표준 라이브러리 `http.server`만으로 만든 초경량 서버. 프레임워크 없음.
- `ThreadingHTTPServer` + `SimpleHTTPRequestHandler` 서브클래스 `Handler`:
  - 정적 파일은 `frontend/` 디렉터리를 루트로 그대로 서빙.
  - `POST /api/generate` 하나만 처리: `{"question": "..."}` 를 받아 `generate.run()`을 호출하고,
    결과 JSON을 통째로 응답으로 돌려준다. (스트리밍이 아니라 **한 번에 전체 결과를 다 계산한 뒤 응답** —
    기획서의 "실시간 스텝 스트리밍(WebSocket)" 대신 **한 번의 HTTP 요청·응답으로 전체 트레이스를
    받은 뒤 프론트에서 그걸 재생**하는 방식으로 단순화되어 있다. 프론트가 "결과를 버퍼에 담아두고
    사람이 볼 수 있는 속도로 재생"한다는 기획 원칙과는 부합하지만, 스텝이 실시간으로 오지 않고
    한꺼번에 온다는 점이 원안과 다르다.)
  - 임시 파일(`NamedTemporaryFile`)에 결과를 썼다가 읽어서 응답하고 바로 삭제. 디스크에 결과가
    남지 않음 (프리렌더 캐싱은 안 됨 — `generate.py`를 직접 CLI로 돌리면 `frontend/data/sample_response.json`에
    저장되는 경로는 남아있지만, 서버 경로에서는 매번 새로 계산해서 버림).
  - Windows 콘솔 인코딩 문제 대응: 콘솔 기본 코드페이지(cp949 등)가 한자 등 특수문자를 못 찍어서
    `print()`가 예외를 던지면 요청을 처리하던 스레드가 응답도 못 하고 죽어버려 "서버가 그냥
    멈춘 것처럼" 보이는 문제가 있었음 → `sys.stdout/stderr.reconfigure(encoding="utf-8", errors="replace")`로 해결.

### 3.2 `generate.py`

모델: `Qwen/Qwen2.5-0.5B-Instruct` (한국어 지원되는 0.5B급 소형 모델). `MAX_NEW_TOKENS=40`,
`TOP_K=6` (후보 개수), `ATTENDED_TOP_N=5` (어텐션 상위 몇 개만 기록).

핵심 함수:

- **`ids_to_pieces(tokenizer, ids)`**: 토큰 id를 하나씩 개별로 `decode`하면, 한글처럼 여러
  바이트가 합쳐져야 완성되는 토큰이 `�`로 깨져 나온다. 그래서 "누적 시퀀스를 매번 통째로
  디코드한 뒤, 이전 결과와 공통되지 않는 새 꼬리만 그 토큰이 보여준 글자로 취급"하는 방식으로
  깨짐 없이 조각을 복원한다. (바이트가 늦게 합쳐지며 앞부분 글자가 나중에 바뀌는 경우까지 대응)
- **`decode_next_piece(tokenizer, base_ids, next_id)`**: 위와 같은 원리로, "다음 토큰 하나만
  추가했을 때 새로 나타나는 글자"를 뽑아 후보 단어 텍스트로 쓴다.
- **`compute_embedding_view(...)`**: 임베딩 장면에서 쓸 좌표를 만든다.
  - `REFERENCE_WORDS`: 감정/자연/시간/정치/경제/과학/가족/예술/종교/교육/직업/건강/동식물/
    도시/여행/언어/의미/색채/운동/추상개념 등 약 20개 범주 × 각 10~15개, 총 300여 개의
    한국어 단어 목록. **매 실행마다 항상 같은 기준 단어들**을 써야, 이번 질문 단어의 벡터가
    그 기준점들 사이 "어디쯽"에 자리 잡는지 비교가 가능해진다.
  - 실제 모델의 입력 임베딩 레이어(`model.get_input_embeddings()`)에서 기준 단어들 + 질문을
    공백 기준으로 나눈 단어들의 벡터를 뽑는다 (여러 토큰으로 쪼개지는 단어는 평균).
  - 기준 단어 벡터와 질문 단어 벡터를 **하나의 행렬로 합쳐서 함께 PCA(`torch.pca_lowrank`, q=2)**
    로 2차원에 투영한다 — 두 그룹을 따로 PCA 하면 서로 다른 좌표계가 되어 비교가 불가능해지므로
    반드시 같은 변환을 같이 적용해야 함.
  - 좌표를 절댓값 최대치로 나눠 `[-1, 1]` 범위로 정규화.
  - 장식이 아니라 **실제 모델 내부 값**을 그대로 쓴다는 점이 이 프로젝트의 신뢰성 포인트.
- **`run(question, out_path)`** — 메인 추론 루프:
  1. `tokenizer.apply_chat_template`으로 채팅 포맷 프롬프트 생성.
  2. `prompt_tokens`(프롬프트를 조각으로 나눈 것), `embedding_view` 계산.
  3. `MAX_NEW_TOKENS`(40)번을 넘지 않는 루프를 돌며, 매 스텝:
     - `model(input_ids, output_attentions=True)` 순전파.
     - 마지막 위치(`logits[0,-1,:]`)의 소프트맥스 확률에서 `topk(k=6)` 뽑아 `candidates`
       (각 `{token, prob, id}`)로 저장. 1등이 곧 `selected`(그리디 디코딩 — 샘플링이 아니라
       항상 최고 확률 토큰을 선택).
     - **어텐션**: 마지막 레이어의 전체 헤드 평균(`outputs.attentions[-1][0]` → heads 축 평균)에서,
       "새로 생성될 위치(-1)가 이전 토큰들에 준 가중치"를 뽑아 내림차순 정렬 후 상위 5개만
       `attended`(`{pos, token, weight}`)로 저장. *(주의: 이 필드는 계산되지만 현재 프론트
       `script.js`에서는 소비하지 않는다 — 기획서 ③ "어텐션 흐름 시각화"가 데이터는 준비돼
       있으나 화면에 아직 안 붙은 상태.)*
     - 선택된 토큰을 시퀀스 끝에 이어붙이고(`input_ids`에 concat) 답변 텍스트에 누적.
  4. **종료 조건**: EOS 토큰이 나오거나, 답변이 **한 문장**으로 끝날 때(`!`, `?`, 또는 `.`이지만
     바로 앞이 숫자가 아닐 때 — `5.25` 같은 소수점/날짜 표현 중간의 마침표는 문장 끝으로
     보지 않음) 멈춘다. 즉 답변은 항상 최대 한 문장으로 제한됨.
  5. 결과를 JSON으로 저장(`out_path`)하고, 스텝 수와 최종 답변을 콘솔에 출력.
- `if __name__ == "__main__"` 블록: CLI로 직접 실행하면 기본 질문("고양이는 왜 그르릉
  소리를 낼까?")을 `frontend/data/sample_response.json`에 저장 — 이 경로가 기획서의
  "프리렌더 모드" 배치 실행 지점에 해당하지만, 지금은 프론트가 이 저장된 파일을 읽어
  재생하는 로직은 없음 (오직 `/api/generate` 실시간 경로만 프론트와 연결돼 있음).

---

## 4. 백엔드 → 프론트엔드 데이터 스키마

`server.py`가 그대로 프론트에 돌려주는 JSON 구조 (`generate.run`의 결과):

```jsonc
{
  "model": "Qwen/Qwen2.5-0.5B-Instruct",
  "question": "입력한 질문 원문",
  "prompt_tokens": ["프롬프트를", "조각낸", "토큰", "..."],
  "prompt_token_ids": [123, 456, ...],
  "embedding": {
    "words":   [{"text": "사랑", "x": -0.12, "y": 0.34}, ...],   // 고정 기준 단어(~300개)
    "myWords": [{"text": "질문단어", "x": 0.5, "y": -0.2}, ...]  // 이번 질문 단어들
  },
  "steps": [
    {
      "step": 0,
      "tokens_so_far": ["지금까지", "누적된", "토큰", "조각들"],
      "attended": [{"pos": 3, "token": "요?", "weight": 0.42}, ...],  // 상위 5개, 현재 프론트 미사용
      "candidates": [
        {"token": "저는", "prob": 0.42, "id": 1234},
        {"token": "오늘", "prob": 0.21, "id": 5678},
        ... // 최대 6개
      ],
      "selected": "저는",
      "selected_id": 1234
    },
    ... // 문장이 끝날 때까지, 최대 40스텝
  ],
  "answer": "모델이 생성한 답변 원문(공백/줄바꿈 포함)"
}
```

프론트는 이 중 `embedding`, `steps[].candidates`, `steps[].selected`, `answer`, `model`을 쓴다.
`prompt_tokens`/`prompt_token_ids`/`attended`는 현재 화면에서 사용되지 않는다
(1단계 "토큰화"는 실제로는 **글자 코드값(codePointAt)을 보여주는 시각적 연출**이고, 모델이
실제로 만든 `prompt_tokens`와는 별개다 — 아래 10장 참고).

---

## 5. 프론트엔드 전체 구조

`index.html`은 딱 하나의 화면(단일 페이지, 라우팅 없음)이고 다음 DOM들로 구성:

| 요소 | 역할 |
|---|---|
| `#resetBtn` (`.txReset`) | "처음으로" — 클릭 시 `resetToForm()` |
| `#stageInfo` (`#stageTitle`, `#stageSub`) | 화면 왼쪽 위, 지금 단계 제목/설명. **SVG가 아니라 HTML** — 카메라가 줌/팬 되어도 크기·위치가 고정되게 하려고 별도 플로팅 위젯으로 뺌 |
| `#traceStage` | 메인 무대. 헤더(`#traceName`,`#metric`)는 실제로는 `display:none`으로 숨겨져 있고, 대신 `stageInfo` + `source`(질문)로 대체됨 |
| `#source` (`.txSource`) | 화면 오른쪽 위, 지금 실행 중인 질문 텍스트 |
| `#traceSvg` | 모든 애니메이션이 그려지는 SVG 캔버스 (`viewBox` 990×500 기준) |
| `#answer` (`.txAnswer`) | 03단계에서 스텝마다 선택된 토큰이 누적되는 "answer stream" 텍스트 |
| `#traceForm` | 최초 질문 입력 폼 (input + RUN 버튼) |
| `#suggestions` | 추천 질문 4개 버튼 (클릭 시 그 문장으로 즉시 제출) |
| `#decoyText` (`.txDecoy`) | 입력창과 완전히 같은 폰트/위치로 만든 가짜 텍스트 — 입력폼→SVG 문장 전환의 다리 역할 (8장 참고) |

스타일(`style.css`)은 검정 배경 + Consolas 계열 모노스페이스 + 흰색/회색 모노크롬 + SVG
필터(`#txGlow`, feGaussianBlur)로 만든 글로우 효과. 기획서 4.4절("검정+모노크롬+글로우,
지금 흐르는 것만 빛나게")을 그대로 구현.

---

## 6. 애니메이션 핵심 개념: "빛(hero) 하나 + 따라오는 카메라"

`script.js`를 이해하는 데 가장 중요한 설계 원칙 두 가지.

### 6.1 화면에는 항상 "빛"이 하나뿐이다 (`heroEl`)

전체 여정 동안 새로운 점을 계속 만들어서 지우는 게 아니라, **`world()`에서 만든 단 하나의
점(`heroEl`, 클래스 `.txHero`)이 그 자체로 주인공(hero)이 되어 처음부터 끝까지 이동**한다.

- 평소에는 `heroWorldX`(레일 위 x좌표)만 바뀌고, 매 프레임 `startFollow()` 루프가
  `heroEl.setAttribute('cx', heroWorldX)`로 화면에 반영한다.
- 어떤 장면이 "빛이 레일을 벗어나 박스 안으로 들어가는" 것처럼 빛을 직접 자유롭게 움직여야
  할 때는 `heroOverride=true`로 두고 그 장면의 코드가 `heroEl`의 `cx`/`cy`를 직접 애니메이션한다
  (`startFollow` 루프는 `heroOverride`가 true인 동안 hero를 건드리지 않음).
- 불투명한 박스 안으로 "가려지며" 들어가는 연출이 필요하면 `heroBehind(g)`로 hero를
  DOM 순서상 박스 그룹(`g`)보다 앞에 두어(SVG는 그려진 순서대로 쌓이므로) 실제로 박스에
  가려지게 하고, 다시 나올 시점에 `heroFront()`로 원래 위치(맨 앞)로 되돌린다.
- 임베딩 장면(11장)처럼 "여러 개의 숫자가 자석처럼 모여 하나의 빛이 되는" 연출도, 실제로는
  임시 점(`orb`)들을 여러 개 만들어 한 점으로 합친 뒤 **그 자리에서 곧바로 hero가 이어받는다**
  — 그래서 "빛이 사라지고 새 빛이 생기는" 틈이 생기지 않는다.

### 6.2 "빛의 이동"과 "화면(카메라)의 이동"을 분리한다 — 2D 플랫포머 카메라

주석 원문 그대로의 설계 의도: *배율(뷰 폭/높이)은 평소 고정한 채, 화면 중앙 대역(데드존)
안에서는 빛이 자기 좌표를 따라 실제로 움직이고, 카메라는 가만히 있는다. 빛이 그 대역을
벗어나려 할 때만 카메라가 뒤따라 움직인다 — 그래서 "빛이 선을 타고 이동"하는 것과
"화면이 움직이는" 것이 뒤섞이지 않고 구분되어 보인다.*

- `VB_W0=990, VB_H0=500`: 기본 뷰박스 크기. `DZ_L=0.35, DZ_R=0.65`: 데드존(화면의 35%~65%
  구간 — 이 안에서는 카메라가 움직이지 않음).
- `camX/camY`(현재 카메라 위치) → `targetX/targetY`(목표 위치)로 매 프레임
  `camRate`(기본 0.11, 시네마틱한 느린 장면에서는 0.045)만큼씩 선형보간 — 부드러운 지수 감쇠.
- `centerOn(x, token)`: 카메라를 직접 옮기는 게 아니라 **hero의 목표 좌표(heroWorldX)를
  ease-out으로 이동시키는** 함수. 카메라는 `startFollow` 루프가 이 이동을 보고 데드존을
  벗어날 때만 따라간다. 이동 시간은 거리에 비례(`320~1100ms`).
- `fitToWidth(centerX, neededW)` / `resetZoom(centerX)`: 문장이 화면보다 넓어지면 뷰박스
  폭·높이를 함께 키워 줌아웃(비율 유지가 핵심 — 폭만 키우면 화면이 눌려 보임), 다시 좁아지면
  원래 배율로 복귀.
- `token`(=`runId`, 7장 참고)이 매 애니메이션 콜백에 전달돼, 새 실행이 시작되면(사용자가
  질문을 다시 보내거나 리셋) 진행 중이던 이전 애니메이션이 스스로 멈춘다.

### 6.3 레일(rail)과 좌표계

- 메인라인(`.txMainRail`)은 SVG 세계 좌표 상의 가로선이며, 시작할 때부터 화면 밖 왼쪽
  멀리(-3000)부터 전체 최대 길이까지 미리 그려져 있다 (계산: `3200+40*640+700+2700` — 최악의
  경우 40스텝까지 다 들어갈 수 있는 길이를 미리 확보).
- `railY` 전역 변수(기본 430)가 "지금 메인라인이 있는 y좌표"를 나타내는데, 임베딩 장면에서
  레일이 아래로 꺾여 내려가면(`embeddingZone`) 이후 값이 바뀌고, 그다음 장면들은 항상
  `railY`를 읽어서 그 높이를 따라간다 — 하드코딩된 430을 쓰지 않음.
- 모든 박스(임베딩 박스, 후보 박스, 최종 응답 박스)는 **"레일에서 위로 올라갔다가 다시
  내려와 합류"**하는 동일한 문법을 반복한다: 레일 → 위(또는 아래)로 이동 → 박스 경계를
  가로지르며 `heroBehind` → 박스 안에서 내용 진행 → 다시 나타나며 `heroFront` → 레일로 복귀.

### 6.4 실행 취소(취소 토큰) 패턴

```js
let runId = 0;
```

`play()`가 시작될 때 `const token = ++runId`로 이번 실행의 고유 번호를 받고, 모든 `await`
직후 `if (token !== runId) return`을 반복해서 검사한다. 사용자가 새 질문을 보내거나
리셋 버튼을 누르면 `runId`가 바뀌므로, 화면에 남아 있는 이전 애니메이션 루프들은 다음
체크포인트에서 조용히 멈춘다 (setTimeout을 강제로 clear하는 대신, "지금도 내가 최신
실행인가"를 스스로 매번 되묻는 방식).

---

## 7. 실행 흐름 한눈에 보기 (`play()`)

```
사용자가 질문 제출
  → (즉시) 입력폼 숨기고 디코이 문장 표시, 백엔드에 /api/generate 요청 시작 (비동기, 기다리지 않음)
  → play(dataPromise, question) 시작
      1) world()            SVG 초기화, 레일/hero 생성, 카메라 팔로우 루프 시작
      2) introZone()        디코이 → 1단계 문장으로 확대, 레일 스윕, "01 TOKENIZATION" 안내 표시
      3) tokenZone()         문장 → 코드값 → 배열 → 빛 (실제 모델 데이터 필요 없음)
      4) await dataPromise   ---- 여기서 처음으로 백엔드 응답을 기다림 ----
      5) embeddingZone()     실제 임베딩 좌표(PCA) 시각화
      6) decodeZone()        스텝별 다음 토큰 후보 경쟁 (steps 배열 반복)
      7) finaleZone()        최종 답변 박스, "05 DETOKENIZATION"
```

핵심 트릭(주석 원문): *1단계(토큰화)는 실제 모델을 전혀 쓰지 않는다(글자를 코드값으로
바꾸는 것뿐) — 그래서 서버 응답을 기다렸다가 시작하지 않고, 요청을 백그라운드로 흘려보낸
채 1단계를 곧장 시작한다. 실제 데이터(임베딩·후보·답변)가 필요한 2단계 직전에만 그 요청이
끝나기를 기다린다 — 1단계가 진행되는 동안 모델 로딩 시간이 자연스럽게 채워진다.*

즉, 백엔드 추론(모델 로드 + 40스텝 forward pass, 수 초 소요)과 프론트 1단계 연출(문장→숫자
변환 애니메이션, 역시 수 초 소요)이 **병렬로 진행**되어 대기 시간이 체감상 사라진다.

---

## 8. 0단계: 입력 → 디코이 전환

`el.form`의 `submit` 핸들러:

1. `preventDefault()` 후 입력값이 비어있지 않은지 확인.
2. RUN을 누르는 즉시(트랜지션 없이) `el.input.getBoundingClientRect()`로 입력창의 **실제
   화면 좌표**를 측정.
3. 폼(`el.form`)과 추천 질문(`el.suggest`)을 `display:none`으로 즉시 숨김.
4. `el.decoy`(`.txDecoy`, 입력창과 완전히 같은 폰트 15px, 같은 위치)에 같은 문장을 넣고 그
   측정한 위치에 정확히 배치, `.show` 클래스로 표시.
5. `requestTrace(q)`로 `/api/generate`에 POST — 이 프로미스는 아직 await하지 않고
   `.catch(()=>{})`만 걸어 unhandled rejection 경고를 막아둔 채 `play()`에 전달.

디코이가 필요한 이유: 입력창(HTML `<input>`)과 이후 SVG `<text>` 요소는 완전히 다른 렌더링
스택이라, 폼이 사라지는 순간과 SVG 문장이 나타나는 순간 사이에 조금이라도 위치/크기가
어긋나면 "튀는" 느낌이 난다. 디코이는 **HTML로 만든 가짜 문장**이라 입력창과 완전히 같은
자리에서 완전히 같은 크기로 나타날 수 있고, 이후 `introZone`이 SVG 좌표계에서 정확히
같은 화면 위치를 역산해 이어받는다.

---

## 9. `introZone` — 도입부(문장 확대 → 레일 스윕)

역할: HTML 디코이 문장을 SVG `<text>` 라벨로 자연스럽게 이어받아, 화면 중앙(y=300)에서
커지고, 메인라인이 스윕되어 나타나고, 단계 안내가 표시되는 도입부.

세부 단계:

1. **좌표 역산**: `el.decoy.getBoundingClientRect()`로 디코이의 화면상 중심(`decoyCx, decoyCy`)을
   구하고, SVG의 `getScreenCTM().inverse()`로 그 화면 좌표를 SVG 세계 좌표로 정확히 변환한다
   (눈대중 보정이 아니라 실측 역산).
2. **크기 일치**: SVG 안의 "1px"은 화면의 실제 픽셀이 아니라 `viewBox` 배율을 타는 사용자
   단위이므로, `svgRect.width/VB_W`와 `svgRect.height/VB_H` 중 더 작은 쪽(레터박스 고려)을
   실제 배율로 써서 "화면에서 정확히 15px로 보이는" `font-size`를 역산한다.
3. 글자 하나하나를 `<tspan>`으로 나눠서 담아 이후 단계(토큰화)에서 개별 글자를 조작할 수
   있게 준비.
4. **미세 보정**: 디코이(박스 중심 기준)와 SVG 텍스트(베이스라인 기준)는 "중심"의 정의가
   다르므로, 공식만으로는 1px 안팎의 오차가 남을 수 있어 실제 렌더링된 두 요소의
   `getBoundingClientRect()` 중심 차이를 측정해 그만큼 y를 보정한다.
5. 같은 프레임에 디코이를 치우고 SVG 라벨을 표시 — "사라짐"과 "나타남" 사이의 틈이 없음.
6. 150ms 대기 후, **같은 엘리먼트**의 `font-size`를 15px → 25px로 0.6초간 부드럽게 키우면서
   동시에 y좌표를 화면 중앙 y=300으로 이동 (새 엘리먼트로 바꿔치기하지 않음 — 하나의
   연속된 움직임으로 보이게).
7. 문장이 화면보다 넓으면 `fitToWidth`로 줌아웃.
8. **레일 스윕**: 메인라인의 시작점을 카메라 왼콽 훨씬 밖(`camX-1200`)까지 접어뒀다가
   0.9초에 걸쳐 화면 오른쪽 끝까지 그려지게 함 — "선이 오른쪽으로 뽑혀 나오는" 느낌.
   끝나면 원래 전체 길이로 즉시 복원(화면 밖이라 변화가 보이지 않음).
9. 단계 안내(`station()`)로 좌상단에 "01 / TOKENIZATION (토큰화)" + 설명 표시, 리셋 버튼
   표시, 우상단에 질문 텍스트(`el.source`) 표시 — 세 가지가 뚝뚝 끊기지 않고 순서대로
   페이드인.

---

## 10. `tokenZone` — 01 TOKENIZATION (7단계)

> 주의: 이 단계는 **실제 토크나이저를 시각화하는 게 아니다.** 입력 문장을 공백 기준
> 단어로 나누고, 각 글자를 유니코드 코드포인트(`codePointAt`)로 바꿔 "글자 하나하나가
> 숫자가 된다"는 개념만 연출로 보여준다. 실제 모델의 서브워드 토큰(`prompt_tokens`)과는
> 무관한 **교육적 시각화**다.

정확히 7단계로 진행 (①은 `introZone`이 이미 보여줬으므로 ②부터 시작):

| 단계 | 내용 |
|---|---|
| ① | 문장이 그대로 보인다 (introZone에서 이어받음) |
| ② | 앞 글자부터 순서대로 두 글자씩 묶어 코드값 숫자로 바뀐다. 지금 바뀌는 묶음에 반전 강조 박스(`.txCursor`)가 따라붙는다 |
| ③ | 전체가 이어진 숫자 하나로 완성 — 개별 글자 엘리먼트로 재구성(단어 구분은 유지) |
| ④ | 글자(숫자) 하나하나가 세로 구분선으로 나뉜다 |
| ⑤ | 구분선이 사라지고 콤마가 끼어들며 숫자들이 실제 렌더링 폭만큼 옆으로 밀려 벌어진다 |
| ⑥ | 공백이 사라지고, 단어 기준 2차원 배열 `[[...], [...]]`로 재구성(괄호·콤마 삽입) |
| ⑦ | 바깥 2차원 배열의 대괄호는 사라지고, 각 단어(1차원 배열)는 자기 괄호가 가운데로
모이며 하나의 점(빛)이 되어 수직으로 메인라인까지 내려간 뒤, 여러 빛이 한 점으로 합쳐져 hero가 된다 |

기술적으로 눈에 띄는 부분:

- ②의 커서 박스는 `g.insertBefore(cursor, label)`로 텍스트보다 먼저(=뒤에) 삽입해야
  글자가 반전된 색으로 그 위에 보인다(순서를 반대로 하면 글자를 가려버림).
- ⑤의 콤마 삽입 후 벌어지는 거리는 **미리 추정하지 않고** `getComputedTextLength()`로
  실제 렌더링 폭을 측정해 목표 위치를 계산 — 추정치를 쓰면 겹치거나 뜨는 오차가 생김.
- ⑥→⑦ 전환에서 첫 단어의 왼쪽 괄호와 마지막 단어의 오른쪽 괄호는 자기 자신의 괄호
  대신 **바깥(2차원) 배열의 괄호와 짝을 이뤄 압축**된다 — 자기 괄호는 그 시점에 페이드아웃.
- ⑦ 이후 각 단어에서 생겨난 여러 개의 임시 점(`orb`)이 메인라인으로 내려온 뒤, 다시
  하나의 점(`mergeX`)으로 모이고, 그 자리에서 **바로 hero가 이어받는다** (6.1절 참고).
- 매 단계마다 `fitToWidth`를 다시 호출해, 괄호·콤마가 추가되어 폭이 늘어날 때마다 카메라가
  실시간으로 맞춰 줌아웃한다.

반환값(`mergeX+250`)은 다음 장면(`embeddingZone`)이 시작할 x좌표.

---

## 11. `embeddingZone` — 02 EMBEDDING FIELD

역할: 메인라인이 아래로 꺾여 거대한 박스로 이어지고, 그 안에 실제 모델의 임베딩 벡터
(PCA 2차원 투영)를 점으로 찍어 보여준다.

1. **레일 꺾기**: 메인라인 1(가로)을 `bendX`에서 끊고, 세로선으로 아래 박스(`boxTop~boxBottom`,
   세로 760)까지 내려간 뒤, 박스 밖에서 다시 가로로 이어지는 메인라인 2를 미리 다 그려둔다
   (카메라가 비추기 전까지는 그냥 화면 밖에 존재). `railY`를 새 높이(`newRailY`)로 갱신 —
   이후 모든 장면이 이 값을 읽음.
2. 박스 안에 좌표 그리드(가로 10칸×세로 6칸, `.txEmbedGrid`)를 얇게 그려 "실제 좌표
   공간 위에 점이 찍힌다"는 느낌을 줌 (장식이지만 의미 있는 장식).
3. `centerOn(bendX)`로 빛을 꺾이는 지점까지 이동시킨 뒤, `heroOverride=true`로 전환해
   레일 아래로 1.5초간 하강시키면서 동시에 카메라를 줌인(`camRate=0.045`, 시네마틱하게
   느리게) — 빛의 실제 하강 속도(1.5초)보다 카메라가 목표를 따라가는 속도가 느리면 빛이
   화면 밖으로 사라져 보이므로, **매 프레임 진행도(e)에 맞춰 목표 카메라 위치(targetY)를
   "빛을 따라가는 값"에서 "최종 박스 프레이밍"으로 서서히 갱신**하며 하강시킴 (단순 보간이
   아니라 진행 상황에 맞춰 목표 자체를 실시간으로 바꾸는 방식).
   - 화면(뷰포트) 전체 기준 중앙과 SVG 엘리먼트의 실제 중앙이 다를 수 있어(SVG가 뷰포트
     전체를 덮지 않음), `getBoundingClientRect()`로 실측한 간극(`viewportCenterGap`)만큼
     y를 보정.
4. 박스 경계를 넘는 순간 `heroBehind(g)`로 hero를 박스에 가려지게 하고, 완전히 들어가면
   `.hidden` 처리.
5. **점 찍기**: 고정 기준 단어(~300개, 흰색 `.txEmbedDot`)를 15ms 간격으로 하나씩
   "번쩍(순간 확대)→가라앉음" 애니메이션으로 찍는다. 점 수가 많아진 만큼 원래보다 5배
   빠른 속도로 찍어 전체를 훑는 총 시간은 비슷하게 유지. 1.5초 쉰 뒤, 이번 질문 단어들
   (`myWords`, 빨간 점 `.txEmbedDotMine`, 크기·글자 모두 2배)이 이어서 같은 방식으로 찍힌다.
   - **`toScreen(nx, ny)`**: PCA로 나온 `[-1,1]` 정규화 좌표를 박스 안의 실제 화면 좌표로
     매핑(양쪽에 `pad=140`의 여백을 둠).
6. 라벨(단어 텍스트)이 서서히 사라진 뒤, 각 점이 **질문 단어들의 무게중심(`mineCx, mineCy`)
   과의 거리 기반 유사도(%)**로 순서대로(4ms 간격) 숫자로 바뀐다 — `sim = (1 - d/maxDist)*100`,
   `maxDist = √2 × 2`(정규화 좌표 공간의 대각선 길이 근사).
7. **소용돌이 합류**: 박스 아래 출구 지점(`vortexX, vortexY`)에 작은 빛을 미리 켜두고,
   모든 숫자가 **거리순(단, ±3 정도의 무작위 순서 오차를 섞어 너무 규칙적으로 보이지
   않게)**으로 그 지점까지 직선으로 빠르게 날아가 붙는다 (stagger 5ms, 개별 비행 140ms,
   ease `t*t*(3-2t)`). 모두 붙은 뒤에야 그 자리에서 hero가 다시 나타나(`heroFront`) 박스
   밖으로 나가는 것으로 이어짐 — vortexLight가 사라지고 hero가 나타나는 사이 틈이 없도록
   같은 프레임에서 처리.
8. 하강 때와 대칭으로, 빛이 ㄴ자(아래→오른쪽)로 새 메인라인(레일 2)에 합류한 뒤
   `heroOverride=false`로 복귀.
9. 카메라를 원래 배율로 복귀시키면서, 다음 장면(후보 박스)이 임베딩 박스(폭 1500)와
   겹치지 않을 만큼 오른쪽으로 충분히 이동(`clearX = exitX + 1000`).

반환값은 다음 장면(`decodeZone`)의 시작 x좌표.

---

## 12. `decodeZone` — 03 NEXT TOKEN (스텝 반복)

역할: 백엔드가 계산해 둔 `data.steps` 배열(문장이 끝날 때까지의 각 토큰 생성 스텝)을
순서대로 재생. 각 스텝마다 후보 4개가 확률 막대로 경쟁하다 1등이 선택되고, 그 토큰이
`el.answer`(answer stream)에 누적된다.

1. **간격 계산**: 박스 간 가로 간격 `gap = clamp(420, 640, 10200/(steps.length-1))` —
   스텝이 많을수록 좁아지지만 최소 420은 보장(박스끼리 거의 붙어 보이지 않게).
2. `data.steps.length`개의 박스를 **미리 전부 생성**(빈 틀만): 각 박스는 `NEXT-TOKEN
   PREDICTION` 라벨, 레일과 이어지는 진입/퇴장 안내선(`.txGuide`)까지 다 그려두고, 내용
   (막대·점멸등)은 그 스텝의 차례가 됐을 때 채운다 — "카메라가 도착하는 순간 갑자기
   생기는" 느낌을 없애기 위함.
3. 스텝 루프(각 스텝마다):
   - `centerOn(x)`로 빛을 이 박스의 중심 x까지 메인라인을 타고 이동.
   - `heroOverride=true` → `heroBehind(g)` → 박스 위쪽에서 아래로(레일→박스 안, 0.28초)
     들어가며 사라짐.
   - **후보 4개**(`step.candidates.slice(0,4)`)를 **확률 순서가 아니라 무작위로 섞은
     세로 순서**(`shuffle`)로 하나씩(85ms 간격) 막대(`.txBar`, 배경 `.txBarBg`), 점멸등
     (`.txLamp`), 후보 텍스트를 나타냄. 막대 폭은 `prob*175`(최소 3px 보장)로 실제 확률에
     비례.
   - **점멸등 경�다**: 실제로 빛이 옆으로 옮겨가는 게 아니라, "켜진 등" 표시(`.on` 클래스)가
     정해진 순서(`hops=[1,3,0,2,3,1,0,2,winnerRow]`)를 따라 이 행 저 행으로 빠르게(28ms →
     105ms로 점점 느려지는 `speeds` 배열) 옮겨붙다가, 실제 1등 후보(무작위로 배치된 자리
     `winnerRow`) 위에서 멈춘다 — 슬롯머신/추첨 연출.
   - 승자 확정: 막대와 텍스트에 `.win` 클래스(하이라이트).
   - 같은 순간 박스 오른쪽 변 가운데에서 hero가 다시 나타남(`heroFront`), 승자 등/막대는
     `.win`→`.done`(옅어짐)으로 전환.
   - hero가 박스 밖으로 나와(90ms) 아래로 내려가(150ms) 다시 메인라인에 합류.
   - `el.answer`에 이번에 선택된 토큰(`step.selected`)을 굵게 이어붙임.
4. 전체 스텝이 끝나면 `el.metric`에 `COMPLETE · N CHARACTERS GENERATED` 표시.

모든 단계 콜백이 `token!==runId` 체크와 `bail()`(hero를 원래 상태로 되돌리는 헬퍼)로
새 실행이 시작되면 즉시 멈추도록 되어 있음.

---

## 13. `finaleZone` — 05 DETOKENIZATION

역할: 마지막 후보 박스를 지난 빛이 메인라인을 타고 더 이동해 거대한 "FINAL OUTPUT" 박스에
도달, 그 안에 실제 모델이 만든 답변 원문이 표시됨.

1. 4초에 걸쳐 메인라인을 타고 진입 지점(`approachX`)까지 이동 (다른 장면보다 훨�다 느리고
   긴 이동 — 마지막이라는 무게감).
2. 다른 박스들과 같은 문법으로 레일→박스 왼쪽 변 높이로 올라간 뒤(0.42초) 옆으로 미끄러져
   들어가며(0.32초) `heroBehind`로 가려짐.
3. 빛이 안전하게 들어간 뒤에야 카메라가 느리게(`camRate=0.045`) 줌아웃해 박스 전체가
   화면 중앙에 오도록 프레이밍. 동시에 `stageInfo`, `source`(질문), `answer`(스트림 텍스트)가
   모두 페이드아웃 — "마지막이니까 주변 UI가 다 사라지고 결과만 남는다".
4. **텍스트 줄바꿈**: 생성 중 보여줬던 자리표시 기호(`·`=공백, `↵`=줄바꿈, `show()` 함수)가
   아니라, 모델이 실제로 만든 원문 그대로(공백/줄바꿈 포함)를 피셀 느낌 폰트(`Galmuri11`)로
   표시. 문장이 길면 세로 공간을 넘칠 수 있으므로:
   - 먼저 기본 크기(34px×camera scale)로 단어 단위 줄바꿈(`wrap()` — 각 줄의 실제
     렌더링 폭을 `measureText`로 측정하며 넘치면 다음 줄로).
   - 줄 수 × 줄 높이(`fitH`)가 박스 안에 다 들어갈 때까지 폰트 크기를 0.9배씩 줄여가며
     재계산(최소 14px×scale까지).
   - 베이스라인 기준 좌표로는 정확한 수직 중앙 정렬이 안 되므로(폰트의 ascent/descent
     때문에), 실제 렌더링된 `getBBox()`를 재서 그 중심을 박스 중앙에 맞추는 보정을
     추가로 적용.
5. 답변이 완전히 자리잡은 뒤에야 `station()`으로 "05 / DETOKENIZATION (평문화)" 안내가
   뒤늦게 페이드인 — *이 전체 과정이 "raw 토큰 출력을 평문으로 바꾼 것"이었다는 설명을
   결과가 다 보인 다음에 붙이는 구성.*

(참고: 단계 번호가 01→02→03→05로 건너뛰는데, 04에 해당하는 "다음 토큰 후보 경쟁"의
반복 자체는 `decodeZone`이 스텝마다 이미 03 라벨을 유지한 채 반복 수행하는 것으로
갈음되고, 명시적인 04 station 텍스트는 따로 없음.)

---

## 14. 실패 처리 · 리셋

- `play()`에서 `dataPromise`가 실패하면(백엔드 연결 실패 등) `resetToForm('추론 서버에
  연결하지 못했습니다. backend/server.py로 실행해야 실제 답변을 생성합니다.')` 호출 —
  1~2단계(문장→숫자 변환)까지는 이미 재생됐더라도, 실제 데이터가 필요한 3단계 진입 전에
  실패를 감지해 안내 문구와 함께 처음 화면으로 되돌린다.
- `resetToForm(hintMsg)`: `runId`를 증가시켜(모든 진행 중 애니메이션 정지) SVG를 완전히
  비우고, 모든 UI 상태(스테이지 클래스, 안내 텍스트, opacity 인라인 스타일 등)를 초기값으로
  되돌린 뒤 입력 폼을 다시 보여주고 입력창에 포커스.
- 리셋 버튼(`#resetBtn`)도 동일한 함수를 호출.

---

## 15. 레거시 파일: `lstm.*`

- `lstm.html`은 `<meta http-equiv="refresh" content="0; url=index.html">`로 **로드 즉시
  index.html로 리다이렉트**만 하는 스텁 — 실질적으로 접근 불가능한 죽은 페이지.
- `lstm.js`(17줄)와 `lstm.css`(6줄)는 `lstm.html`이 참조하지 않으므로 죽은 코드. 내용을
  보면 기획서 초반의 "LSTM 회로도(게이트 σ/tanh, cell state/hidden state)" 스타일을 그대로
  구현한 **초기 프로토타입**으로 보인다(회로 셀 `makeCell`, 게이트 기호 `gate()`, 후보 경쟁
  코너 위젯 등) — 지금의 트랜스포머 트레이스(`script.js`) 이전 버전의 유물.
- `frontend/lib/three.module.js`도 현재 아무 파일에서 import되지 않는 미사용 라이브러리.

정리 대상이지만 굳이 지울 필요는 없음 — 참고용으로 남겨둔 것으로 보임.

---

## 16. 현재 상태 요약 / 남은 것

**구현된 것**
- 백엔드: 실제 Qwen2.5-0.5B-Instruct 모델로 그리디 디코딩 추론, 스텝별 후보/확률/어텐션/
  임베딩 PCA 좌표까지 실데이터 계산, 한 문장 제한 종료 조건, HTTP API 하나로 서빙.
- 프론트: 단일 SVG 무대 위에서 "빛 하나 + 데드존 팔로우 카메라"로 이어지는 5단계
  연출(문장→토큰화 연출→임베딩 실데이터 시각화→다음 토큰 후보 경쟁 반복→최종 답변) 완성.
  실행 취소 토큰(runId)으로 중간에 새 요청이 와도 안전하게 전환.

**기획 대비 아직 없는 것 / 달라진 것**
- 어텐션 "선(레이어 사이 빛나는 연결)" 시각화 — 데이터(`attended`)는 뽑고 있지만 화면에
  그리지 않음.
- WebSocket 기반 스텝 단위 실시간 스트리밍 — 지금은 백엔드가 전체를 다 계산한 뒤 한 번에
  응답, 프론트가 그걸 재생하는 방식.
- 프리렌더 모드(추천 질문을 사전 계산해 캐싱) — `generate.py`를 CLI로 단독 실행하면
  JSON 파일로 저장은 되지만, 프론트가 그 파일을 읽어 재생하는 경로는 아직 없음(현재
  "추천 질문" 버튼도 실시간 API를 그대로 호출).
- 마스코트 디자인/등장 연출 — 기획서에서도 범위 밖으로 명시.
- 답변이 항상 "한 문장"으로 제한되어 있음(종료 조건에 하드코딩) — 여러 문장 답변은
  현재 구조상 나오지 않음.
