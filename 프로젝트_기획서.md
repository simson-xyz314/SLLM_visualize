# sLLM 응답 과정 시각화 프로그램 기획서 (초안)

## 1. 컨셉

관객이 입력한 질문 하나가 sLLM(소형 언어모델) 안에서 답으로 만들어지는 과정을 실시간으로 시각화한다.
핵심 메시지는 하나다 — **"AI는 답을 한 번에 만드는 게 아니라, 한 조각(토큰)씩 이어 붙인다."**

레퍼런스 영상은 LSTM 기반의 작은 모델로 이 과정을 회로도처럼 시각화했지만, 요즘 sLLM은 대부분 트랜스포머 구조이므로 본 프로그램은 **트랜스포머의 셀프 어텐션 흐름**을 시각화 대상으로 삼는다. 안내가 필요한 지점에는 레퍼런스의 노란 막대인간 대신 별도로 디자인한 마스코트를 사용한다(디자인은 본 기획 범위 밖, 추후 결정).

## 2. 관객 경험 흐름

```
질문 입력
   ↓
① 토큰화 — 문장이 서브워드 조각으로 쪼개지고 각 조각에 숫자 ID가 붙는다
   ↓
② 임베딩 — 각 토큰이 벡터(점/색)로 변환된다
   ↓
③ 어텐션 흐름 — 층이 쌓인 슬랩 사이로 토큰끼리 빛나는 선(어텐션 가중치)이 이어진다
   ↓
④ 다음 토큰 후보 경쟁 — 여러 후보 단어가 확률 막대로 경쟁하다 하나가 선택된다
   ↓
⑤ 선택된 토큰이 입력 끝에 붙고, ③~④ 반복 (토큰 단위 생성 루프)
   ↓
⑥ 답변 완성
```

레퍼런스 장면과의 대응:

| 레퍼런스 장면 | 본 프로그램 |
|---|---|
| ASCII 변환 (글자→숫자) | 한국어 질문 → 서브워드 토큰 → 토큰 ID |
| LSTM 셀 회로도 (게이트, h₀·h₁·h₂) | 트랜스포머 레이어 슬랩 사이 어텐션 선 |
| Epoch/Accuracy 카운터 | (학습이 아니라 추론이므로 미사용 — 대신 스텝 카운터로 "몇 번째 토큰 생성 중"을 표시) |
| 캐릭터가 입력을 들고 들어감 | 질문 입력 UI, 자체 마스코트로 대체 |

## 3. 구현 방식 — 하이브리드(C안)

- **추천 질문 모드**: 미리 정한 질문 5~6개는 사전에 모델을 돌려 토큰/어텐션/확률 데이터를 계산해두고, 동일한 렌더러로 재생한다. 완성도가 가장 높은 시나리오이므로 기본/대기 화면으로 사용.
- **자유 입력 모드**: 관객이 직접 입력하면 같은 백엔드·같은 렌더러가 실시간으로 처리한다.
- **핵심 설계 원칙**: 렌더러(프론트엔드)는 "사전 계산 데이터"와 "실시간 계산 데이터"를 **동일한 JSON 스키마**로 받아 소비한다. 즉 프리렌더 모드는 실시간 모드의 출력을 미리 캐싱해둔 것일 뿐, 별도 파이프라인이 아니다. 이렇게 하면 자유 입력에서 문제가 생겨도 추천 질문 모드로 즉시 폴백할 수 있다.

## 4. 시스템 아키텍처

### 4.1 백엔드 (Python)
- Hugging Face `transformers`로 모델 로드
- 생성 시 `output_attentions=True`, `output_hidden_states=True`로 내부 값 추출
- 매 스텝마다:
  - 어텐션 가중치 (레이어별/헤드별 → 시각화를 위해 헤드 평균 또는 대표 레이어 선택)
  - 상위 5~10개 다음 토큰 후보와 확률
  - 선택된 토큰
- 위 데이터를 토큰 단위로 WebSocket을 통해 JSON으로 프론트엔드에 스트리밍
- 프리렌더 모드는 동일 로직을 오프라인 배치로 돌려 JSON 파일로 저장 → 프론트가 파일을 읽어 동일 렌더러로 재생

### 4.2 프론트엔드
- 후보: `three.js`/`p5.js` (웹 기반, 배포·유지보수 용이) vs `TouchDesigner`/`Unity` (미디어아트 표현력, 설치 환경 친화적)
- 임베딩 벡터(수백~수천 차원)는 PCA로 2~3차원 축소 후 점/막대/색으로 표현
- 어텐션은 토큰 노드 사이의 빛나는 선(굵기·밝기 = 가중치 크기)으로 표현
- 다음 토큰 후보는 확률 막대 경쟁 → 승자 하이라이트 → 입력 줄 끝에 합류하는 애니메이션
- 재생 속도: 실제 추론은 순식간에 끝나므로, 결과를 버퍼에 모두 담아둔 뒤 사람이 따라갈 수 있는 속도로 재생 (스텝 간 딜레이는 조정 가능한 파라미터로 둔다)

### 4.3 모델
- 한국어를 지원하는 1B 전후 소형 모델 후보 (Qwen 소형 계열, EXAONE 소형 버전, Kanana 소형 버전 등) — 설치 시점에 최신 모델 재확인 필요
- 일반 게이밍 GPU급 하드웨어로 추론 가능한 크기를 기준으로 선정

### 4.4 시각 스타일 (레퍼런스 스크린샷 기준)

사용자가 공유한 레퍼런스 화면 캡처를 기준으로 톤을 정리한다.

- **배경/색**: 검정 배경 + 모노크롬(흰색·회색) + 글로우. 색은 거의 쓰지 않고, 지금 값이 흐르고 있는 요소만 빛나게 해서 시선을 자연스럽게 유도한다.
- **코드 텍스처**: 실제로 동작하는 백엔드 코드 일부(예: 토큰화 함수, forward 함수)를 화면 배경에 작고 흐릿하게 깔아 "진짜로 계산되고 있다"는 신뢰감을 준다. 장식이 아니라 실제 프로젝트 코드에서 발췌.
- **회로도형 레이아웃**: 레퍼런스는 LSTM 체인(h₀, h₁, h₂ … h₁₄)을 게이트 기호(σ, tanh, ×, +)와 함께 회로처럼 가로로 펼쳐 놓고, 값이 흐르는 지점을 글로우 노드(빛나는 점)로 표시한다. 본 프로그램은 이 레이아웃 문법을 그대로 가져오되 내용을 **트랜스포머 레이어 슬랩 + 토큰 간 어텐션 선**으로 교체한다 (섹션 2·4.2 참고).
- **상태 라벨/카운터**: "Tokenizer", "Training Data" 같은 작은 캡션과 "Epoch 100/1000, Accy: 0.9415" 같은 진행 상태 텍스트를 diagram 옆에 배치. 본 프로그램에서는 "토큰 생성 스텝 n/N", "Attention", "Next Token Candidates" 등으로 대체해 지금 어떤 단계인지 알려준다.
- **안내 캐릭터**: 레퍼런스의 노란 막대인간(원작자 캐릭터, 그대로 쓰면 저작권 문제)은 자체 마스코트로 교체한다. 마스코트 디자인 자체는 이 기획서 범위 밖(섹션 7 참고)이지만, 등장 문법(질문을 들고 화면 안으로 들어가는 연출)은 참고할 만하다.

## 5. 데이터 스키마 (초안)

프론트가 소비할 스텝 단위 JSON 예시:

```json
{
  "step": 3,
  "tokens_so_far": ["안녕", "하세", "요", "?"],
  "attention": {
    "layer": "avg",
    "matrix": [[...]]
  },
  "candidates": [
    {"token": "저는", "prob": 0.42},
    {"token": "오늘", "prob": 0.21},
    {"token": "네", "prob": 0.15}
  ],
  "selected": "저는"
}
```

이 스키마를 실시간/프리렌더 양쪽에서 동일하게 사용한다.

## 6. 구현 단계 (프로그램 관점)

1. **Phase 1 — 모델 래퍼**: 모델 로드, 스텝별 어텐션/후보확률 추출 함수 작성, JSON 스키마 확정 → 검증: 터미널에서 질문 1개 넣고 스텝별 JSON이 스키마대로 나오는지 확인
2. **Phase 2 — 프리렌더 파이프라인**: 추천 질문 5~6개를 배치로 돌려 JSON 저장 → 검증: 저장된 JSON을 프론트 없이 읽어 스텝 수·후보 개수가 말이 되는지 확인
3. **Phase 3 — 프론트 렌더러 프로토타입**: 저장된 JSON 하나를 재생하는 최소 뷰어 (토큰화 → 어텐션 → 후보 경쟁 → 선택, 4단계) → 검증: 사람이 눈으로 각 단계를 구분할 수 있는 속도/레이아웃인지
4. **Phase 4 — 실시간 연동**: WebSocket으로 백엔드-프론트 연결, 자유 입력 모드 추가 → 검증: 자유 입력 시 프리렌더와 동일한 렌더러로 동일하게 동작하는지
5. **Phase 5 — 비주얼 폴리싱**: 검정 배경 + 흑백/포인트 컬러 톤, 마스코트 삽입, 활성 요소만 빛나게 하는 연출 다듬기

## 7. 결정이 필요한 항목 (Open Questions)

- 프론트엔드 기술 선택: 웹(three.js/p5.js) vs 미디어아트 툴(TouchDesigner/Unity) — 설치 환경(웹캠/터치 유무, 화면 형태)에 따라 달라짐
- 사용 모델 최종 선정 (한국어 성능·속도·라이선스 확인 필요)
- 마스코트 디자인 (본 기획 범위 밖)
- 어텐션 시각화 시 레이어/헤드를 어떻게 요약해 보여줄지 (평균? 특정 레이어? 관객 이해도를 우선할지 기술적 정확도를 우선할지)

## 8. 범위에서 제외한 것

사용자 요청에 따라 전시 운영 계획(관람 시간 제한, 부적절 입력 필터, 안내 자막, 인력 배치 등)은 이 기획서에 포함하지 않았다. 필요 시 별도로 다룬다.
